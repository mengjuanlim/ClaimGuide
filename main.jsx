import React,{useEffect,useState} from 'react';import{createRoot}from'react-dom/client';import{ShieldCheck,ArrowRight,UploadCloud,FileText,Search,Plus,LogOut,Home,FolderOpen,Settings,BookOpen,CheckCircle2,AlertTriangle,MessageSquareText,Save,Download,Eye,LockKeyhole,Facebook,X}from'lucide-react';import'./styles.css';
const API=import.meta.env.VITE_API_URL||'';const call=async(url,opt={})=>{const r=await fetch(API+url,{credentials:'include',headers:{...(opt.body instanceof FormData?{}:{'Content-Type':'application/json'}),...opt.headers},...opt});if(!r.ok)throw new Error((await r.json().catch(()=>({}))).error||'Request failed');return r.json()};
function Logo(){return <div className="logo"><span><ShieldCheck/></span><div><b>ClaimGuide</b><small>Claims clarity, when it matters</small></div></div>}
function Landing({login}){return <><header className="top"><Logo/><nav><a href="#how">How it works</a><a href="#security">Security</a><button className="ghost" onClick={login}>Log in</button><button onClick={login}>Start a claim</button></nav></header><main><section className="hero"><div><p className="pill">AI-assisted claim preparation</p><h1>Take control of your insurance claim.</h1><p className="lead">Review your policy, organise supporting documents and prepare a clear, evidence-linked response to your insurer in one secure workspace.</p><button onClick={login}>Get started <ArrowRight size={17}/></button></div><div className="preview"><div className="row"><div><small>CLAIM CG-10241</small><h3>Water damage at warehouse</h3></div><em>Action needed</em></div><div className="grid2"><article><ShieldCheck/><b>Policy review</b><p>Relevant provisions identified</p></article><article><FileText/><b>Evidence gaps</b><p>Items needing attention</p></article></div><article className="wide"><MessageSquareText/><b>Response ready to refine</b></article></div></section><section id="how" className="section"><h2>From scattered documents to a structured response</h2><div className="grid3">{[['Upload your claim file','Policy, letters, reports and invoices.'],['Understand the issues','Source-linked provisions and evidence gaps.'],['Prepare your response','Editable drafts with human review.']].map((x,i)=><article key={x[0]}><strong>0{i+1}</strong><h3>{x[0]}</h3><p>{x[1]}</p></article>)}</div></section><section id="security" className="dark"><LockKeyhole/><h2>Your claim documents deserve serious protection.</h2><p>Testing build includes authenticated sessions, ownership checks, upload limits and security headers. Configure TLS, production secrets, backups and cloud object storage before public use.</p></section></main></>}
function Login({done,back,demo}){const oauth=p=>location.href=API+'/auth/'+p;return <div className="auth"><div className="authbrand"><Logo/><h1>A clearer way to prepare your insurance claim.</h1><p>Keep your policy, evidence and correspondence together.</p></div><div className="authbox"><button className="link" onClick={back}>← Back</button><h2>Welcome to ClaimGuide</h2><p>Log in or create your account.</p><button className="oauth" onClick={()=>oauth('google')}>G&nbsp;&nbsp; Continue with Google</button><button className="facebook" onClick={()=>oauth('facebook')}><Facebook size={18}/> Continue with Facebook</button>{demo&&<><div className="or">or</div><button className="demo" onClick={async()=>{await call('/auth/demo',{method:'POST'});done()}}>Use testing account</button></>}<small>By continuing, you accept the Terms, Privacy Notice and AI limitations. Social login requires configured provider credentials.</small></div></div>}
function NewClaim({created,cancel}){const[f,setF]=useState({title:'',claimType:'Property damage',insurer:'',incidentDate:''});const submit=async()=>created(await call('/api/claims',{method:'POST',body:JSON.stringify(f)}));return <div className="panel form"><button className="link" onClick={cancel}>← Dashboard</button><h2>Start a new claim</h2><label>Claim title<input value={f.title} onChange={e=>setF({...f,title:e.target.value})} placeholder="Water damage at warehouse"/></label><label>Claim type<select value={f.claimType} onChange={e=>setF({...f,claimType:e.target.value})}>{['Property damage','Marine or cargo','Liability claim','Work injury','Motor claim','Other'].map(x=><option>{x}</option>)}</select></label><label>Insurer<input value={f.insurer} onChange={e=>setF({...f,insurer:e.target.value})}/></label><label>Incident date<input type="date" value={f.incidentDate} onChange={e=>setF({...f,incidentDate:e.target.value})}/></label><button disabled={!f.title} onClick={submit}>Create workspace</button></div>}
function Claim({id,back}){const[c,setC]=useState(null),[tab,setTab]=useState('overview'),[draft,setDraft]=useState(''),[file,setFile]=useState(null);const load=()=>call('/api/claims/'+id).then(setC);useEffect(load,[id]);if(!c)return <p>Loading…</p>;const upload=async()=>{const fd=new FormData();fd.append('file',file);await call(`/api/claims/${id}/documents`,{method:'POST',body:fd});setFile(null);load()};const generate=async()=>setDraft((await call(`/api/claims/${id}/generate`,{method:'POST',body:'{}'})).content);const save=async()=>call(`/api/claims/${id}/drafts`,{method:'POST',body:JSON.stringify({mode:'balanced',content:draft})});return <div><button className="link" onClick={back}>← All claims</button><div className="claimhead"><div><small>{c.ref}</small><h1>{c.title}</h1><p>{c.claim_type} • {c.insurer||'Insurer not entered'} • {c.incident_date||'Date not entered'}</p></div><em>{c.status}</em></div><div className="tabs">{['overview','policy','documents','response','activity'].map(x=><button className={tab===x?'active':''} onClick={()=>setTab(x)}>{x}</button>)}</div>{tab==='overview'&&<div className="grid2"><article className="panel"><h3>Claim progress</h3><div className="progress"><i style={{width:c.progress+'%'}}/></div><p>{c.progress}% complete</p></article><article className="panel"><h3>Next actions</h3><p>Upload the complete policy wording, incident chronology and itemised quantum documents.</p></article><article className="panel span"><h3>Review findings</h3><div className="finding good"><CheckCircle2/> Insuring clause may be relevant, subject to verified facts and complete wording.</div><div className="finding warn"><AlertTriangle/> Notification timing and repair breakdown need supporting evidence.</div></article></div>}{tab==='policy'&&<div className="panel"><h2>Policy review</h2><p className="notice">This testing build provides a structured review workspace. It does not make a coverage decision or provide legal advice.</p><div className="grid3"><article><b>Potentially responsive</b><p>Record the insuring clause and source page.</p></article><article><b>Conditions</b><p>Check notification, cooperation and mitigation duties.</p></article><article><b>Possible exclusions</b><p>Record exact wording and facts relevant to application.</p></article></div></div>}{tab==='documents'&&<div className="panel"><div className="row"><h2>Documents</h2><label className="upload"><UploadCloud size={17}/> Select file<input type="file" onChange={e=>setFile(e.target.files[0])}/></label></div>{file&&<button onClick={upload}>Upload {file.name}</button>}<div className="docs">{c.documents.map(d=><div><FileText/><span><b>{d.name}</b><small>{d.category} • {d.review_status}</small></span><a href={`${API}/api/documents/${d.id}/download`}><Download size={16}/></a></div>)}</div></div>}{tab==='response'&&<div className="panel"><div className="row"><h2>Draft response</h2><button onClick={generate}>Generate template</button></div><textarea rows="18" value={draft} onChange={e=>setDraft(e.target.value)} placeholder="Generate or write your response…"/><button onClick={save}><Save size={16}/> Save version</button></div>}{tab==='activity'&&<div className="panel"><h2>Activity</h2>{c.activities.map(a=><div className="activity"><i/><span><b>{a.event}</b><small>{a.created_at} • {a.actor}</small></span></div>)}</div>}</div>}
function App(){const[me,setMe]=useState(undefined),[view,setView]=useState('dashboard'),[claims,setClaims]=useState([]),[open,setOpen]=useState(null);const refresh=()=>call('/api/me').then(setMe);useEffect(()=>{refresh()},[]);useEffect(()=>{if(me?.user)call('/api/claims').then(setClaims)},[me,view,open]);if(me===undefined)return null;if(!me.user){if(view==='login')return <Login demo={me.demo} done={refresh} back={()=>setView('landing')}/>;return <Landing login={()=>setView('login')}/>};if(open)return <Shell me={me.user} logout={async()=>{await call('/auth/logout',{method:'POST'});setMe({user:null,demo:me.demo})}}><Claim id={open} back={()=>setOpen(null)}/></Shell>;return <Shell me={me.user} logout={async()=>{await call('/auth/logout',{method:'POST'});setMe({user:null,demo:me.demo})}}>{view==='new'?<NewClaim cancel={()=>setView('dashboard')} created={c=>{setOpen(c.id)}}/>:<><div className="dashhead"><div><small>GOOD MORNING</small><h1>Your claims workspace</h1><p>Continue an existing claim or start a guided review.</p></div><button onClick={()=>setView('new')}><Plus size={17}/> Start a claim</button></div><div className="stats"><article><b>{claims.length}</b><span>Total claims</span></article><article><b>{claims.filter(x=>x.status==='Action needed').length}</b><span>Needs action</span></article><article><b>{claims.filter(x=>x.status==='Submitted').length}</b><span>Submitted</span></article></div><div className="panel"><div className="row"><h2>My claims</h2><Search/></div>{claims.length?claims.map(c=><button className="claimrow" onClick={()=>setOpen(c.id)}><span><b>{c.title}</b><small>{c.ref} • {c.claim_type} • {c.insurer||'No insurer'}</small></span><em>{c.status}</em><ArrowRight/></button>):<div className="empty"><FolderOpen/><h3>No claims yet</h3><button onClick={()=>setView('new')}>Create your first claim</button></div>}</div></>}</Shell>}
function Shell({me,logout,children}){return <><header className="top"><Logo/><div className="user"><span>{me.name}</span><button className="ghost" onClick={logout}><LogOut size={16}/> Log out</button></div></header><div className="shell"><aside><a><Home/>Dashboard</a><a><FolderOpen/>My claims</a><a><BookOpen/>Resources</a><a><Settings/>Settings</a></aside><main>{children}</main></div></>};createRoot(document.getElementById('root')).render(<App/>);
